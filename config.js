// GitHub Pages can stay static. Set this to the deployed backend WebSocket URL.
// Example: wss://solo-card-game-api.example.com/ws
window.SOLO_CARD_GAME_CONFIG = window.SOLO_CARD_GAME_CONFIG || { wsUrl: '' };
